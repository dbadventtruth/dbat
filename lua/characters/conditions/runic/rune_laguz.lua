local function modifiers(ch, cond)
    local mods = {}

    return mods
end

return {
    id = "rune_laguz",
    name = "Laguz Rune",
    tags = { "rune_laguz" },
    persistent = true,
    modifiers = modifiers,
}
