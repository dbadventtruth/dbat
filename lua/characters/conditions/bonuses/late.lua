return {
    id         = "bonus_late",
    name       = "Late",
    tags       = { "bonus" },
    persistent = false,
    description = "Late Sleeper - Can only wake automatically. 33% every hour if maxed",
}
