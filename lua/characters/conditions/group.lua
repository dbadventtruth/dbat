local function modifiers(ch, cond)
    local mods = {}

    return mods
end

return {
    id = "group",
    name = "Group",
    tags = { "group" },
    persistent = true,
    modifiers = modifiers,
}
