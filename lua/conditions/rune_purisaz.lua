local function modifiers(ch, cond)
    local mods = {}

    return mods
end

return {
    id = "rune_purisaz",
    name = "Purisaz Rune",
    tags = { "rune_purisaz" },
    persistent = true,
    modifiers = modifiers,
}
