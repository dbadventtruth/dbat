local function modifiers(ch, cond)
    local mods = {}


    return mods
end

return {
    id = "healing_glow",
    name = "Healing Glow",
    tags = { "healing_glow" },
    persistent = true,
    modifiers = modifiers,
}
